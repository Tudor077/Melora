# Production/release builds ship WITHOUT a baked-in Client ID so every user
# supplies their own Spotify app (bring-your-own-Client-ID). Local `npm run
# dev` still uses the value in .env.
VITE_SPOTIFY_CLIENT_ID=
